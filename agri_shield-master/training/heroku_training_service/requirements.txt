# Heroku Training Service Requirements
# Optimized for Heroku's 500MB slug size limit

# Core Flask
Flask==2.3.0
gunicorn==21.2.0

# Database
pymysql==1.1.0

# Machine Learning - Use ONNX Runtime for inference (smaller)
# For training, we'll use PyTorch but with optimizations
# Note: PyTorch is large (~450MB), consider using CPU-only version
torch==2.0.0+cpu
torchvision==0.15.0+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Image Processing
Pillow==10.0.0
numpy==1.24.3

# Optional: scikit-learn for metrics (if needed)
# scikit-learn==1.3.0  # Uncomment if needed

# Note: Full PyTorch with CUDA is ~450MB
# CPU-only version is ~150MB (fits Heroku better)
# For GPU training, consider using Google Colab or separate GPU service

